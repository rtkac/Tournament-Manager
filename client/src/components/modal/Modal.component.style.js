import { styled } from 'baseui';

export const ModalBodyDiv = styled('div', () => ({
  display: 'flex',
}));

export const ModalBodyIcon = styled('span', () => ({
  marginRight: '.5rem',
  width: 30,
}));

export const ModalBodyPar = styled('p', () => ({
  display: 'flex',
  marginTop: '.25rem',
}));
