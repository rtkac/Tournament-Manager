import { styled } from 'baseui';

export const HeaderDiv = styled('div', () => ({
  display: 'flex',
  margin: '0 auto',
  maxWidth: '1315px',
  padding: '0 1rem',
  width: '100%',
}));

export const HeaderLinkButton = styled('span', () => ({
  cursor: 'pointer',
}));
